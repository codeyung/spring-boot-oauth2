<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.ddgs.dao.AccountDao">

    <resultMap id="roles" type="com.ddgs.model.Account">
        <id property="id" column="id"/>
        <result property="name" column="name"/>
        <result property="password" column="password"/>
        <collection property="roles" ofType="com.ddgs.model.Role"
                    column="id">
            <id column="rId" property="id"/>
            <result column="roleName" property="roleName"/>
        </collection>
    </resultMap>


    <select id="findByName" resultMap="roles">
       select a.id,a.name,a.password,c.id as rId ,c.roleName from Account a,AccountRole b ,Role c
          where a.name = #{name} and a.id = b.aId and b.rId = c.id;
    </select>

</mapper>