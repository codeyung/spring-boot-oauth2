<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.ddgs.dao.OAuth2Dao">


    <select id="checkClientId" resultType="boolean">
         select count(*) from oauth_client_details where client_id = #{client_id};
    </select>

    <select id="checkClientSecret" resultType="boolean">
         select count(*) from oauth_client_details where client_secret = #{client_secret};
    </select>

    <select id="getAIdByClientId" resultType="string">
         select aId from oauth_client_details where client_id = #{client_id};
    </select>

</mapper>