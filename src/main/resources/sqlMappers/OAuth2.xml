<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.ddgs.dao.OAuth2Dao">


    <select id="checkClientId" resultType="boolean">
         select count(*) from OAuth2Client where client_id = #{client_id};
    </select>

    <select id="checkClientSecret" resultType="boolean">
         select count(*) from OAuth2Client where client_secret = #{client_secret};
    </select>

    <select id="getClientIdByName" resultType="string">
        select b.client_id from Account a , OAuth2Client b where a.id = b.aId and a.username = #{username};
    </select>

</mapper>